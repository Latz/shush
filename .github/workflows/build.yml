name: Build
on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - uses: actions/setup-node@v4
        with:
          # 22, not 20: the popup uses ES2025 iterator helpers (.values().toArray()),
          # which Node only gained in 22. Chrome has had them since 122, the version
          # manifest.json declares as the minimum, so this is a test-runtime floor only.
          node-version: '22'
      - run: npm ci
      # Before the tests: lint is seconds, so a style break fails fast rather than after
      # the full suite. Note that no-unused-vars is a warning, and warnings do not fail
      # eslint — add --max-warnings 0 to the lint script if they should.
      - run: npm run lint
      - run: npm run test:coverage
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@fd88b7d7ccbaefd23d8f36f73b59db7a3d246602 # v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          
