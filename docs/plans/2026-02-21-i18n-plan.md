# i18n Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add Chrome-native i18n to all user-facing strings across the Shush! extension for en, de, fr, es, ja, zh_CN.

**Architecture:** Chrome MV3's `_locales/{locale}/messages.json` + `chrome.i18n.getMessage()` in JS + `__MSG_key__` in manifest. No libraries. 14 string keys. Branded strings ("Shush!", "Unshush!") stay hardcoded in English everywhere.

**Tech Stack:** Chrome i18n API (`chrome.i18n.getMessage`), `_locales` directory convention, MV3 manifest `default_locale`

---

### Task 1: Create `_locales` message files for all 6 locales

**Files:**
- Create: `_locales/en/messages.json`
- Create: `_locales/de/messages.json`
- Create: `_locales/fr/messages.json`
- Create: `_locales/es/messages.json`
- Create: `_locales/ja/messages.json`
- Create: `_locales/zh_CN/messages.json`

**Step 1: Create the directory structure**

```bash
mkdir -p _locales/en _locales/de _locales/fr _locales/es _locales/ja _locales/zh_CN
```

**Step 2: Create `_locales/en/messages.json`**

```json
{
  "extDescription": { "message": "Find and silence tabs making noise in the background." },
  "popupScanning":  { "message": "Scanning for noisy tabs..." },
  "noAudio":        { "message": "No tabs are playing audio. All quiet!" },
  "audioCurrentTab":{ "message": "All audio is coming from the current tab." },
  "btnSwitch":      { "message": "Switch" },
  "untitled":       { "message": "Untitled" },
  "errorLoadTabs":  { "message": "Error loading tabs. Please try again." },
  "menuFindNoisyTabs": { "message": "Find Noisy Tabs" },
  "menuCurrentTab": { "message": "(current tab)" },
  "menuMuted":      { "message": "(muted)" },
  "menuSwitchToTab":{ "message": "Switch to Tab" },
  "menuMuteTab":    { "message": "Mute Tab" },
  "menuUnmuteTab":  { "message": "Unmute Tab" },
  "errorScanTabs":  { "message": "Error scanning tabs. Please try again." }
}
```

**Step 3: Create `_locales/de/messages.json`**

```json
{
  "extDescription": { "message": "Finde und stumm schalte Tabs, die im Hintergrund Geräusche machen." },
  "popupScanning":  { "message": "Suche nach lauten Tabs..." },
  "noAudio":        { "message": "Keine Tabs spielen Audio ab. Alles ruhig!" },
  "audioCurrentTab":{ "message": "Das gesamte Audio kommt vom aktuellen Tab." },
  "btnSwitch":      { "message": "Wechseln" },
  "untitled":       { "message": "Ohne Titel" },
  "errorLoadTabs":  { "message": "Fehler beim Laden der Tabs. Bitte erneut versuchen." },
  "menuFindNoisyTabs": { "message": "Laute Tabs finden" },
  "menuCurrentTab": { "message": "(aktueller Tab)" },
  "menuMuted":      { "message": "(stummgeschaltet)" },
  "menuSwitchToTab":{ "message": "Zum Tab wechseln" },
  "menuMuteTab":    { "message": "Tab stumm schalten" },
  "menuUnmuteTab":  { "message": "Tab Ton einschalten" },
  "errorScanTabs":  { "message": "Fehler beim Scannen der Tabs. Bitte erneut versuchen." }
}
```

**Step 4: Create `_locales/fr/messages.json`**

```json
{
  "extDescription": { "message": "Trouvez et réduisez au silence les onglets bruyants en arrière-plan." },
  "popupScanning":  { "message": "Recherche des onglets bruyants..." },
  "noAudio":        { "message": "Aucun onglet ne diffuse de l'audio. Tout est calme !" },
  "audioCurrentTab":{ "message": "Tout l'audio provient de l'onglet actuel." },
  "btnSwitch":      { "message": "Basculer" },
  "untitled":       { "message": "Sans titre" },
  "errorLoadTabs":  { "message": "Erreur lors du chargement des onglets. Veuillez réessayer." },
  "menuFindNoisyTabs": { "message": "Trouver les onglets bruyants" },
  "menuCurrentTab": { "message": "(onglet actuel)" },
  "menuMuted":      { "message": "(muet)" },
  "menuSwitchToTab":{ "message": "Aller à l'onglet" },
  "menuMuteTab":    { "message": "Couper le son" },
  "menuUnmuteTab":  { "message": "Rétablir le son" },
  "errorScanTabs":  { "message": "Erreur lors de l'analyse des onglets. Veuillez réessayer." }
}
```

**Step 5: Create `_locales/es/messages.json`**

```json
{
  "extDescription": { "message": "Encuentra y silencia las pestañas que hacen ruido en segundo plano." },
  "popupScanning":  { "message": "Buscando pestañas ruidosas..." },
  "noAudio":        { "message": "Ninguna pestaña está reproduciendo audio. ¡Todo en silencio!" },
  "audioCurrentTab":{ "message": "Todo el audio proviene de la pestaña actual." },
  "btnSwitch":      { "message": "Cambiar" },
  "untitled":       { "message": "Sin título" },
  "errorLoadTabs":  { "message": "Error al cargar las pestañas. Por favor, inténtalo de nuevo." },
  "menuFindNoisyTabs": { "message": "Buscar pestañas ruidosas" },
  "menuCurrentTab": { "message": "(pestaña actual)" },
  "menuMuted":      { "message": "(silenciado)" },
  "menuSwitchToTab":{ "message": "Ir a la pestaña" },
  "menuMuteTab":    { "message": "Silenciar pestaña" },
  "menuUnmuteTab":  { "message": "Activar sonido" },
  "errorScanTabs":  { "message": "Error al analizar las pestañas. Por favor, inténtalo de nuevo." }
}
```

**Step 6: Create `_locales/ja/messages.json`**

```json
{
  "extDescription": { "message": "バックグラウンドで音を出しているタブを見つけて消音します。" },
  "popupScanning":  { "message": "音のあるタブを検索中..." },
  "noAudio":        { "message": "音を再生しているタブはありません。静かです！" },
  "audioCurrentTab":{ "message": "すべての音は現在のタブから出ています。" },
  "btnSwitch":      { "message": "切り替え" },
  "untitled":       { "message": "タイトルなし" },
  "errorLoadTabs":  { "message": "タブの読み込みエラー。もう一度お試しください。" },
  "menuFindNoisyTabs": { "message": "音のあるタブを探す" },
  "menuCurrentTab": { "message": "（現在のタブ）" },
  "menuMuted":      { "message": "（ミュート中）" },
  "menuSwitchToTab":{ "message": "タブに切り替え" },
  "menuMuteTab":    { "message": "タブをミュート" },
  "menuUnmuteTab":  { "message": "ミュートを解除" },
  "errorScanTabs":  { "message": "タブのスキャンエラー。もう一度お試しください。" }
}
```

**Step 7: Create `_locales/zh_CN/messages.json`**

```json
{
  "extDescription": { "message": "查找并静音在后台播放声音的标签页。" },
  "popupScanning":  { "message": "正在扫描嘈杂的标签页..." },
  "noAudio":        { "message": "没有标签页正在播放音频。一切安静！" },
  "audioCurrentTab":{ "message": "所有音频均来自当前标签页。" },
  "btnSwitch":      { "message": "切换" },
  "untitled":       { "message": "无标题" },
  "errorLoadTabs":  { "message": "加载标签页时出错，请重试。" },
  "menuFindNoisyTabs": { "message": "查找嘈杂的标签页" },
  "menuCurrentTab": { "message": "（当前标签页）" },
  "menuMuted":      { "message": "（已静音）" },
  "menuSwitchToTab":{ "message": "切换到标签页" },
  "menuMuteTab":    { "message": "静音标签页" },
  "menuUnmuteTab":  { "message": "取消静音" },
  "errorScanTabs":  { "message": "扫描标签页时出错，请重试。" }
}
```

**Step 8: Verify all 6 files exist and are valid JSON**

```bash
for f in _locales/en _locales/de _locales/fr _locales/es _locales/ja _locales/zh_CN; do
  python3 -c "import json; json.load(open('$f/messages.json')); print('$f OK')"
done
```

Expected: 6 lines each ending with `OK`. Any `JSONDecodeError` means a syntax mistake in that file.

**Step 9: Commit**

```bash
git add _locales/
git commit -m "feat: add _locales message files for en, de, fr, es, ja, zh_CN"
```

---

### Task 2: Update `manifest.json` — add `default_locale` and use `__MSG_extDescription__`

**Files:**
- Modify: `manifest.json`

**Context:** Chrome requires `"default_locale"` in manifest.json whenever `_locales/` exists. Without it the extension fails to load. The extension name and action title stay as plain `"Shush!"` (brand — not localised).

**Step 1: Edit `manifest.json`**

The full new `manifest.json`:

```json
{
  "manifest_version": 3,
  "name": "Shush!",
  "version": "1.0.0",
  "description": "__MSG_extDescription__",
  "default_locale": "en",
  "permissions": [
    "tabs",
    "notifications",
    "activeTab",
    "windows",
    "contextMenus",
    "scripting"
  ],
  "host_permissions": ["<all_urls>"],
  "action": {
    "default_title": "Shush!",
    "default_popup": "popup.html"
  },
  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  },
  "background": {
    "service_worker": "background-simple.js"
  }
}
```

**Step 2: Verify**

Load the extension in Chrome (`chrome://extensions` → reload). It should load without errors. The description shown on the extensions page should read "Find and silence tabs making noise in the background."

If Chrome shows "Could not load extension" with "default_locale" in the error, double-check that `_locales/en/messages.json` exists and is valid.

**Step 3: Commit**

```bash
git add manifest.json
git commit -m "feat: add default_locale and localise description in manifest"
```

---

### Task 3: Update `popup.js` to use `chrome.i18n.getMessage()`

**Files:**
- Modify: `popup.js`

**Context:** `chrome.i18n.getMessage('key')` is synchronous and available in popup scripts. If a key is missing it returns `''` (empty string) — easy to spot during testing. "Shush!" and "Unshush!" button labels are intentionally NOT localised.

**Step 1: Replace hardcoded strings**

Full new `popup.js`:

```js
// Popup script for Shush! extension

async function loadNoisyTabs() {
  const content = document.getElementById('content');
  content.innerHTML = `<div class="no-tabs">${chrome.i18n.getMessage('popupScanning')}</div>`;

  try {
    const [allTabs, currentWindow] = await Promise.all([
      chrome.tabs.query({}),
      chrome.windows.getCurrent()
    ]);
    // Get the current window's active tab from the already-fetched allTabs
    const currentActiveTab = allTabs.find(t => t.active && t.windowId === currentWindow.id);

    // Check each tab for audio
    const noisyTabsList = [];
    let totalAudioTabs = 0;

    for (const tab of allTabs) {
      // Skip tabs without URLs (like chrome:// pages)
      if (!tab.url || !tab.url.startsWith('http')) continue;

      const isAudible = tab.audible;
      const isActiveInCurrentWindow = currentActiveTab && tab.id === currentActiveTab.id;

      if (isAudible) {
        totalAudioTabs++;
        if (!isActiveInCurrentWindow) {
          noisyTabsList.push({
            id: tab.id,
            title: tab.title || chrome.i18n.getMessage('untitled'),
            url: tab.url,
            favIconUrl: tab.favIconUrl || '',
            muted: tab.mutedInfo?.muted || false
          });
        }
      }
    }

    // Handle different scenarios
    if (totalAudioTabs === 0) {
      content.innerHTML = `<div class="no-tabs">${chrome.i18n.getMessage('noAudio')}</div>`;
    } else if (noisyTabsList.length === 0) {
      content.innerHTML = `<div class="no-tabs">${chrome.i18n.getMessage('audioCurrentTab')}</div>`;
    } else {
      // Show noisy tabs
      content.innerHTML = '';
      noisyTabsList.forEach(tab => {
        const cleanTitle = tab.title.replace(/^\(\d+\)\s*/, '');
        const tabTitle = cleanTitle.length > 30 ? cleanTitle.substring(0, 27) + '...' : cleanTitle;

        const item = document.createElement('div');
        item.className = 'tab-item';

        if (tab.favIconUrl) {
          const img = document.createElement('img');
          img.className = 'tab-favicon';
          img.src = tab.favIconUrl;
          img.addEventListener('error', () => { img.style.visibility = 'hidden'; });
          item.appendChild(img);
        }

        const titleDiv = document.createElement('div');
        titleDiv.className = 'tab-title';
        titleDiv.title = cleanTitle;
        titleDiv.textContent = tabTitle;
        item.appendChild(titleDiv);

        const actions = document.createElement('div');
        actions.className = 'tab-actions';

        const switchBtn = document.createElement('button');
        switchBtn.className = 'switch-btn';
        switchBtn.textContent = chrome.i18n.getMessage('btnSwitch');
        switchBtn.addEventListener('click', () => {
          chrome.tabs.update(tab.id, { active: true });
          window.close();
        });
        actions.appendChild(switchBtn);

        const muteBtn = document.createElement('button');
        muteBtn.className = tab.muted ? 'unmute-btn' : 'mute-btn';
        muteBtn.textContent = tab.muted ? 'Unshush!' : 'Shush!';
        muteBtn.addEventListener('click', async () => {
          const nowMuted = !tab.muted;
          try {
            // Delegate mute to background service worker to avoid popup-context revert
            const response = await chrome.runtime.sendMessage({ action: 'muteTab', tabId: tab.id, muted: nowMuted });
            const actuallyMuted = response?.muted ?? nowMuted;
            tab.muted = actuallyMuted;
            muteBtn.textContent = actuallyMuted ? 'Unshush!' : 'Shush!';
            muteBtn.className = actuallyMuted ? 'unmute-btn' : 'mute-btn';
          } catch (err) {
            console.error('Mute failed:', err);
          }
        });
        actions.appendChild(muteBtn);

        item.appendChild(actions);
        content.appendChild(item);
      });
    }
  } catch (error) {
    console.error('Error loading noisy tabs:', error);
    content.innerHTML = `<div class="no-tabs">${chrome.i18n.getMessage('errorLoadTabs')}</div>`;
  }
}

// Load tabs when popup opens
document.addEventListener('DOMContentLoaded', loadNoisyTabs);
```

**Step 2: Verify**

Reload the extension. Open the popup with no audio playing — should show "No tabs are playing audio. All quiet!" (English). Open the popup's DevTools console and run:

```js
chrome.i18n.getMessage('noAudio')
```

Expected: `"No tabs are playing audio. All quiet!"`. If it returns `""`, the key is missing from `_locales/en/messages.json`.

**Step 3: Commit**

```bash
git add popup.js
git commit -m "feat: localise popup.js strings via chrome.i18n"
```

---

### Task 4: Update `background-simple.js` — localise strings and fix stale notification title

**Files:**
- Modify: `background-simple.js`

**Context:** `chrome.i18n.getMessage()` works in MV3 service workers. Two places use the same message keys as popup.js (`noAudio`, `audioCurrentTab`). Also fixes a stale bug: one `chrome.notifications.create` call uses the old name `"Where's the Noise"` as `title` — replace with `"Shush!"`.

**Step 1: Replace all hardcoded strings**

Diff of changes needed (apply these to `background-simple.js`):

| Line (approx) | Old value | New value |
|---|---|---|
| `title: "Find Noisy Tabs"` (×3) | `"Find Noisy Tabs"` | `chrome.i18n.getMessage('menuFindNoisyTabs')` |
| `' (current tab)'` | `' (current tab)'` | ` ${chrome.i18n.getMessage('menuCurrentTab')}` |
| `' (muted)'` | `' (muted)'` | ` ${chrome.i18n.getMessage('menuMuted')}` |
| `"Switch to Tab"` | `"Switch to Tab"` | `chrome.i18n.getMessage('menuSwitchToTab')` |
| `"Mute Tab"` / `"Unmute Tab"` | literal strings | `chrome.i18n.getMessage('menuMuteTab')` / `chrome.i18n.getMessage('menuUnmuteTab')` |
| `message: 'No tabs are playing audio. All quiet!'` | literal | `chrome.i18n.getMessage('noAudio')` |
| `message: 'All audio is coming from the current tab.'` | literal | `chrome.i18n.getMessage('audioCurrentTab')` |
| `title: "Where's the Noise"` | stale bug | `"Shush!"` |
| `message: 'Error scanning tabs. Please try again.'` | literal | `chrome.i18n.getMessage('errorScanTabs')` |

Full new `background-simple.js`:

```js
// Handle mute requests from popup (avoids popup-context revert behaviour)
chrome.runtime.onMessage.addListener((message, _sender, sendResponse) => {
  if (message.action === 'muteTab') {
    chrome.tabs.update(message.tabId, { muted: message.muted })
      .then(tab => {
        const actualMuted = tab.mutedInfo?.muted ?? message.muted;
        injectMediaMute(message.tabId, actualMuted);
        sendResponse({ muted: actualMuted });
      })
      .catch(() => sendResponse({ muted: message.muted }));
    return true; // keep channel open for async response
  }
});

function injectMediaMute(tabId, muted) {
  chrome.scripting.executeScript({
    target: { tabId, allFrames: true },
    func: (m) => {
      document.querySelectorAll('audio, video').forEach(el => {
        el.muted = m;
        // Some players pause in response to el.muted=true; resume them on unmute
        if (!m && el.paused && !el.ended) el.play().catch(() => {});
      });
    },
    args: [muted]
  }).catch(() => {}); // silently ignore restricted pages (chrome://, PDFs, etc.)
}

chrome.contextMenus.onClicked.addListener((info) => {
  if (info.menuItemId === "find-noisy-tabs") {
    scanAndShowResults();
  } else if (info.menuItemId.endsWith("-switch")) {
    const tabId = parseInt(info.menuItemId.replace("-switch", "").replace("noisy-tab-", ""), 10);
    if (Number.isFinite(tabId) && tabId > 0) {
      chrome.tabs.update(tabId, { active: true });
    }
  } else if (info.menuItemId.endsWith("-mute")) {
    const tabId = parseInt(info.menuItemId.replace("-mute", "").replace("noisy-tab-", ""), 10);
    if (Number.isFinite(tabId) && tabId > 0) {
      chrome.tabs.get(tabId)
        .then(t => {
          const nowMuted = !t.mutedInfo?.muted;
          return chrome.tabs.update(tabId, { muted: nowMuted })
            .then(updated => injectMediaMute(tabId, updated.mutedInfo?.muted ?? nowMuted));
        })
        .catch(() => {}); // tab may have closed between menu click and handler
    }
  }
  // else: click on a noisy-tab-N parent label (current tab or background tab title) — no action
});

// Background service worker for Shush! extension
// Handles badge tracking and context menu interactions

chrome.runtime.onInstalled.addListener(() => {
  chrome.contextMenus.create({
    id: "find-noisy-tabs",
    title: chrome.i18n.getMessage('menuFindNoisyTabs'),
    contexts: ["all"]
  }, () => {
    if (chrome.runtime.lastError) {
      console.error('Error creating context menu:', chrome.runtime.lastError);
    }
  });
  updateAll();
});

chrome.runtime.onStartup.addListener(() => {
  updateAll();
});

// Listen for tab close to update badge and menu
chrome.tabs.onRemoved.addListener(() => {
  scheduleUpdate();
});

// Listen for tab updates to track audio state
// Use declarative event filter where supported; fall back to JS-side check (e.g. Vivaldi)
try {
  chrome.tabs.onUpdated.addListener(() => {
    scheduleUpdate();
  }, { properties: ['audible'] });
} catch (e) {
  chrome.tabs.onUpdated.addListener((tabId, changeInfo) => {
    if (changeInfo.audible !== undefined) scheduleUpdate();
  });
}

// Listen for tab activation to update badge and menu
chrome.tabs.onActivated.addListener(() => {
  scheduleUpdate();
});

// Single debounced update replacing scheduleBadgeUpdate + scheduleMenuUpdate
let updateTimeout;
function scheduleUpdate() {
  clearTimeout(updateTimeout);
  updateTimeout = setTimeout(() => updateAll(), 500);
}

// Fetch tabs data once and update both badge and menu in a single pass
async function updateAll() {
  try {
    const [allTabs, [currentActiveTab]] = await Promise.all([
      chrome.tabs.query({}),
      chrome.tabs.query({ active: true, lastFocusedWindow: true })
    ]);

    // Update badge
    const audioCount = allTabs.filter(t => t.audible).length;
    if (audioCount > 0) {
      chrome.action.setBadgeText({ text: audioCount.toString() });
      chrome.action.setBadgeBackgroundColor({ color: '#000000' });
    } else {
      chrome.action.setBadgeText({ text: '' });
    }

    // Update menu
    const noisyTabsList = buildNoisyTabsList(allTabs, currentActiveTab);
    if (noisyTabsList.length === 0) {
      chrome.contextMenus.removeAll(() => {
        chrome.contextMenus.create({
          id: "find-noisy-tabs",
          title: chrome.i18n.getMessage('menuFindNoisyTabs'),
          contexts: ["all"]
        }, () => { if (chrome.runtime.lastError) console.error('Context menu error:', chrome.runtime.lastError); });
      });
    } else {
      await showNoisyTabsInMenu(noisyTabsList);
    }
  } catch (error) {
    console.error('Update error:', error);
  }
}

// Pure function — builds noisy tabs list from pre-fetched data
function buildNoisyTabsList(allTabs, currentActiveTab) {
  const noisyTabsList = [];
  for (const tab of allTabs) {
    if (!tab.url || !tab.url.startsWith('http')) continue;
    if (tab.audible) {
      noisyTabsList.push({
        id: tab.id,
        title: tab.title || chrome.i18n.getMessage('untitled'),
        muted: tab.mutedInfo?.muted || false,
        isCurrentTab: currentActiveTab && tab.id === currentActiveTab.id
      });
    }
  }
  return noisyTabsList;
}

async function scanAndShowResults() {
  try {
    const [allTabs, [currentActiveTab]] = await Promise.all([
      chrome.tabs.query({}),
      chrome.tabs.query({ active: true, lastFocusedWindow: true })
    ]);
    const noisyTabsList = buildNoisyTabsList(allTabs, currentActiveTab);
    const backgroundNoisyTabs = noisyTabsList.filter(t => !t.isCurrentTab);

    if (noisyTabsList.length === 0) {
      chrome.notifications.create({
        type: 'basic',
        iconUrl: 'icons/icon48.png',
        title: "Shush!",
        message: chrome.i18n.getMessage('noAudio')
      });
    } else if (backgroundNoisyTabs.length === 0) {
      chrome.notifications.create({
        type: 'basic',
        iconUrl: 'icons/icon48.png',
        title: "Shush!",
        message: chrome.i18n.getMessage('audioCurrentTab')
      });
    } else {
      await showNoisyTabsInMenu(noisyTabsList);
    }
  } catch (error) {
    console.error('Scan error:', error);
    chrome.notifications.create({
      type: 'basic',
      iconUrl: 'icons/icon48.png',
      title: "Shush!",
      message: chrome.i18n.getMessage('errorScanTabs')
    });
  }
}

// NOTE: Menu state is not persisted across service worker restarts. If the worker
// is killed while the menu is in its expanded state (after a scan), the menu will
// remain expanded until the user clicks "Find Noisy Tabs" again. This is an
// unavoidable consequence of storing menu state only in Chrome's context menu registry.
function showNoisyTabsInMenu(noisyTabsList) {
  return new Promise((resolve) => {
    chrome.contextMenus.removeAll(() => {
      chrome.contextMenus.create({
        id: "find-noisy-tabs",
        title: chrome.i18n.getMessage('menuFindNoisyTabs'),
        contexts: ["all"]
      }, () => { if (chrome.runtime.lastError) console.error('Context menu error:', chrome.runtime.lastError); });

      chrome.contextMenus.create({
        id: "separator",
        type: "separator",
        contexts: ["all"]
      }, () => { if (chrome.runtime.lastError) console.error('Context menu error:', chrome.runtime.lastError); });

      noisyTabsList.forEach((tab) => {
        const cleanTitle = tab.title.replace(/^\(\d+\)\s*/, '');
        const tabTitle = cleanTitle.length > 30 ? cleanTitle.substring(0, 27) + '...' : cleanTitle;
        const itemId = `noisy-tab-${tab.id}`;
        const currentLabel = tab.isCurrentTab ? ` ${chrome.i18n.getMessage('menuCurrentTab')}` : '';
        const mutedLabel = tab.muted ? ` ${chrome.i18n.getMessage('menuMuted')}` : '';

        chrome.contextMenus.create({
          id: itemId,
          title: `${tabTitle}${currentLabel}${mutedLabel}`,
          contexts: ["all"]
        }, () => { if (chrome.runtime.lastError) console.error('Context menu error:', chrome.runtime.lastError); });

        if (!tab.isCurrentTab) {
          chrome.contextMenus.create({
            id: `${itemId}-switch`,
            parentId: itemId,
            title: chrome.i18n.getMessage('menuSwitchToTab'),
            contexts: ["all"]
          }, () => { if (chrome.runtime.lastError) console.error('Context menu error:', chrome.runtime.lastError); });

          chrome.contextMenus.create({
            id: `${itemId}-mute`,
            parentId: itemId,
            title: tab.muted ? chrome.i18n.getMessage('menuUnmuteTab') : chrome.i18n.getMessage('menuMuteTab'),
            contexts: ["all"]
          }, () => { if (chrome.runtime.lastError) console.error('Context menu error:', chrome.runtime.lastError); });
        }
      });

      // Chrome serialises contextMenus operations within a single callback, so
      // resolve() is called after all creates have been queued and will execute
      // in order — no need to await each individual create.
      resolve();
    });
  });
}
```

**Step 2: Verify**

Reload the extension. Open the service worker console (`chrome://extensions` → Shush! → "Service Worker" link → Console). Run:

```js
chrome.i18n.getMessage('menuFindNoisyTabs')
```

Expected: `"Find Noisy Tabs"`. Right-click any page — the context menu entry should read "Find Noisy Tabs".

**Step 3: Commit**

```bash
git add background-simple.js
git commit -m "feat: localise background-simple.js strings via chrome.i18n; fix stale notification title"
```

---

### Task 5: Final smoke test

**No files changed — verification only.**

**Step 1: Confirm extension loads cleanly**

Go to `chrome://extensions`. Shush! should show no errors. If there's a red error badge, click "Errors" to read it — most likely a missing `default_locale` or a typo in a message key.

**Step 2: Verify all locale files have the same keys**

```bash
python3 -c "
import json, os, sys
base = json.load(open('_locales/en/messages.json'))
keys = set(base.keys())
ok = True
for locale in ['de', 'fr', 'es', 'ja', 'zh_CN']:
    path = f'_locales/{locale}/messages.json'
    data = json.load(open(path))
    missing = keys - set(data.keys())
    extra   = set(data.keys()) - keys
    if missing: print(f'{locale} MISSING: {missing}'); ok = False
    if extra:   print(f'{locale} EXTRA:   {extra}');   ok = False
if ok: print('All locales have identical key sets.')
"
```

Expected: `All locales have identical key sets.`

**Step 3: Verify no hardcoded English UI strings remain in JS**

```bash
grep -n "'Scanning\|All quiet\|current tab\|Find Noisy\|Switch to Tab\|Mute Tab\|Unmute Tab\|Error loading\|Error scanning\|Where's the Noise" popup.js background-simple.js
```

Expected: no output (all replaced with `chrome.i18n.getMessage` calls).

**Step 4: Commit**

No code changes — nothing to commit if steps 1-3 pass.
